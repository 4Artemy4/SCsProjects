//создаем некое пространство с именем graph 
//добавляем еще одно пространство G1 пространство graph
graph -> G1;;

//добавляем G1 2 элемента, которые представляют из себя множества (вершин и ребер)
G1 -> rrel_arcs: ..arcs;;
G1 -> rrel_nodes: ..nodes;;

//добавляем во множество вершин вершины
..nodes -> V0;;
..nodes -> V1;;
..nodes -> V2;;

//и дуги во множество дуг 
..arcs -> (V0 <=> V1);;
..arcs -> (V1 <=> V3);;
